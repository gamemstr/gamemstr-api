<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Locations</title>
    </head>
    <body>
        <section id = "hello">
            <h1>Locations</h1>
            {{#each locations}}
            <ul>
                <li>ID: {{this.id}}</li>
                <li>Name: {{this.name}}</li>
                <li>Description: {{this.description}}</li>
                <li>World ID: {{this.world_id}}</li>
                <li>Coordinates: {{this.map_coordinates}}</li>
                <li>NPCs: {{this.npcs}}</li>
            </ul>
            {{/each}}
        </section>
    </body>
</html>